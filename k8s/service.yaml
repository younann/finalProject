apiVersion: v1
kind: Service
metadata:
  name: python-app-service
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster  # AWS EKS Optimization
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

